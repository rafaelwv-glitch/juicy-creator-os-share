/**
 * Local email/password sign-in (this app's Better Auth DB — not the broker).
 *
 * On: the lounge is locked behind a real account so dashboard data can be
 * user-bound across browsers. JuicyChat credentials are stored separately
 * (encrypted) and can be pushed from the Android companion via a pairing code.
 */
export const emailAndPasswordEnabled = true;
