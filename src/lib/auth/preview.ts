/**
 * Shareable clone: OAuth preview credentials are intentionally empty.
 * This build ships with auth OFF (`VITE_AUTH_ENABLED=false`). Do not put
 * live client secrets here.
 */
export const PREVIEW_CLIENT_ID = "";
export const PREVIEW_CLIENT_SECRET = "";
export const GROK_ISSUER_DEFAULT = "";
export const PREVIEW_ALLOWED_HOSTS = [] as const;
